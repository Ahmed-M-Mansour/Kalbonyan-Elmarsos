let studentScore = 18;
let maxScorr = 20;
let percent = (studentScore / maxScorr ) * 100;
console.log(percent);

let studentScore = 18;
let maxScore = 20;

let percentage = (studentScore / maxScore) * 100;

console.log(percentage);